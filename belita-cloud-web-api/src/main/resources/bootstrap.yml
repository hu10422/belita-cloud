spring:
  application:
    name: order-web
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: nacos.lc:8848
        namespace: ${spring.profiles.active}
        file-extension: yaml
      discovery:
        server-addr: nacos.lc:8848
        namespace: ${spring.profiles.active}
      username: nacos
      password: nacos

#debug: on
