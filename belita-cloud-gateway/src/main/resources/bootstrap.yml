server:
  port: 80

spring:
  application:
    name: balita-gateway
  cloud:
    gateway:
      routes:
        - id: order-index
          uri: http://127.0.0.1:81
          predicates:
            - Path=/**